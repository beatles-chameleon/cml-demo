<view class="cml-radio-group  cml-base cml-view cml-1add4249" style="{{horizontalStyle}}">
  <cml-buildin-radio a:for="{{radioList}}" color="{{item.color || color}}" data-index="{{ index }}" data-uid="{{ item.uid }}" checked="{{ item.checked }}" label="{{ item.label }}" group-index="{{ index }}" position="{{ subPosition }}" disabled="{{ item.disabled ? true : false }}" onChange="_cmlEventProxy" data-eventchange="{{['valueChange']}}" cstyle="{{item.style || itemStyle}}" class=" cml-base cml-radio cml-1add4249">
  </cml-buildin-radio>
</view>