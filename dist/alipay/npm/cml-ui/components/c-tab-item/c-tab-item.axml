<view class="{{cTabsItem}}  cml-base cml-view cml-cf528bde" onTap="_cmlEventProxy" data-eventtap="handleTabTap">
  <view a:if="{{tab.prefixStyle}}" style="{{finalPrefix}}" class=" cml-base cml-view cml-cf528bde"></view>
  <text style="{{finalLabel}}" class=" cml-base cml-text cml-cf528bde">{{tab.label}}</text>
  <view a:if="{{tab.suffixStyle}}" style="{{finalSuffix}}" class=" cml-base cml-view cml-cf528bde"></view>
</view>