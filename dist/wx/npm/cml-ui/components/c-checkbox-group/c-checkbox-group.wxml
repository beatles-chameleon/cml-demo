<view class="cml-checkbox-group  cml-base cml-view" style="{{horizontalStyle}}">
  <cml-buildin-checkbox wx:for="{{ option }}" data-index="{{ index }}" data-uid="{{ item.uid }}" checked="{{ item.checked }}" label="{{ item.label }}" group-index="{{ index }}" position="{{ subPosition }}" disabled="{{ item.disabled ? true : false }}" bindchange="_cmlEventProxy" data-eventchange="valueChange" class=" cml-base cml-checkbox">
  </cml-buildin-checkbox>
</view>